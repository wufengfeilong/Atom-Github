<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="fcn.co.jp.park.mapper.manager.WaterFeeMapper" >

  <update id="updateByPrimaryKey" parameterType="fcn.co.jp.park.util.PageData" >
    UPDATE t_water_electric
    SET start_num = #{STARTNUM,jdbcType=VARCHAR},
      end_num = #{ENDNUM,jdbcType=VARCHAR},
      record_date = #{RECORDDATE,jdbcType=VARCHAR},
      cost_num = #{COSTNUM,jdbcType=VARCHAR},
      unit_price = #{UNITPRICE,jdbcType=VARCHAR},
      fee = #{FEE,jdbcType=VARCHAR},
    WHERE id = #{WATERFEEID,jdbcType=INTEGER}
  </update>

  <select id="getWaterFeeList" parameterType="fcn.co.jp.park.util.PageData" resultType="fcn.co.jp.park.util.PageData">
  SELECT
  id AS waterFeeId,
  "公司名称" AS companyName,
  "xue.hongxia@fujisoft-china.com" AS companyMail,
  start_num AS startNum,
  end_num AS endNum,
  cost_num AS costNum,
  unit_price AS unitPrice,
  record_date AS recordDate,
  fee AS fee,
  pay_status AS isPay
  FROM t_water_electric
  WHERE type = "1"
  LIMIT #{numS},#{numE}
</select>

  <select id="getWaterFeeListCount"  resultType="java.lang.Integer">
    SELECT
      count(0)
    FROM
      t_water_electric
    WHERE type = "1"
  </select>
</mapper>