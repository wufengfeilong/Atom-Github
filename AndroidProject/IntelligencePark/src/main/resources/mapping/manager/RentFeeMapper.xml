<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="fcn.co.jp.park.mapper.manager.RentFeeMapper" >

  <update id="updateByPrimaryKey" parameterType="fcn.co.jp.park.util.PageData" >
    UPDATE t_rent_fee
    SET area = #{COMPANYSPACE,jdbcType=VARCHAR},
      unit_price = #{UNITPRICE,jdbcType=VARCHAR},
      rentfee_startDate = #{STARTDATE,jdbcType=VARCHAR},
      rentfee_endDate = #{ENDDATE,jdbcType=VARCHAR},
      discount = #{DISCOUNT,jdbcType=VARCHAR},
      fee = #{FEE,jdbcType=VARCHAR},
      remark = #{COMMENT,jdbcType=VARCHAR}
    WHERE rentfee_id = #{ID,jdbcType=INTEGER}
  </update>

  <select id="getRentFeeList" parameterType="fcn.co.jp.park.util.PageData" resultType="fcn.co.jp.park.util.PageData">
  SELECT
  rentfee_id AS rentFeeId,
  "公司名称" AS companyName,
  "xue.hongxia@fujisoft-china.com" AS companyMail,
  rentfee_startDate AS startDate,
  rentfee_endDate AS endDate,
  area AS companySpace,
  unit_price AS unitPrice,
  discount AS discount,
  fee AS fee,
  remark AS comment,
  pay_status AS isPay,
  0 AS delFlag
  FROM t_rent_fee
  LIMIT #{numS},#{numE}
</select>

  <select id="getRentFeeListCount"  resultType="java.lang.Integer">
    SELECT
      count(0)
    FROM
      t_rent_fee
  </select>
</mapper>