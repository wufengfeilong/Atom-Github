<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="fcn.co.jp.park.mapper.manager.AdvertisingFeeEditMapper">

    <!-- 查询套餐类型的内容 -->
    <select id="getAdvertisingFeeEditData" parameterType="fcn.co.jp.park.util.PageData" resultType="fcn.co.jp.park.util.PageData">
        select distinguish_key as advertiseSetType, distinguish_id as advertiseSetId, distinguish_name as advertiseSetName, distinguish_value as advertiseSetFee
        from m_distinguish
        where distinguish_key = 'ADVERTISE_SET_TYPE'
        order by distinguish_id
    </select>

    <!-- 更新广告套餐一费用的信息 -->
    <update id="updateAdvertisingSet1FeeData" parameterType="fcn.co.jp.park.util.PageData">
        update m_distinguish
        set
        distinguish_value = #{advertiseSet1Fee,jdbcType=VARCHAR}
        where distinguish_key = 'ADVERTISE_SET_TYPE' and distinguish_id = '0'
    </update>
    <!-- 更新广告套餐二费用的信息 -->
    <update id="updateAdvertisingSet2FeeData" parameterType="fcn.co.jp.park.util.PageData">
        update m_distinguish
        set
        distinguish_value = #{advertiseSet2Fee,jdbcType=VARCHAR}
        where distinguish_key = 'ADVERTISE_SET_TYPE' and distinguish_id = '1'
    </update>
    <!-- 更新广告套餐三费用的信息 -->
    <update id="updateAdvertisingSet3FeeData" parameterType="fcn.co.jp.park.util.PageData">
        update m_distinguish
        set
        distinguish_value = #{advertiseSet3Fee,jdbcType=VARCHAR}
        where distinguish_key = 'ADVERTISE_SET_TYPE' and distinguish_id = '2'
    </update>
</mapper>